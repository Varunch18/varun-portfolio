// vite.config.ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react-swc";
import path from "path";
import { componentTagger } from "lovable-tagger";

// https://vitejs.dev/config/
export default defineConfig(({ mode }) => ({
  // === THIS LINE IS CRUCIAL FOR PUBLISHING YOUR WEBSITE ===
  // It sets the base path for all your project's assets.
  // It's conditional to work both locally and on GitHub Pages.
  base: mode === 'production' ? '/varun-portfolio/' : '/',
  // ========================================================
  server: {
    host: "::",
    port: 8080,
  },
  plugins: [
    react(),
    mode === 'development' &&
    componentTagger(),
  ].filter(Boolean),
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
}));